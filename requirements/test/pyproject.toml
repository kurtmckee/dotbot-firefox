[tool.poetry]
package-mode = false

[tool.poetry.dependencies]
python = ">=3.8"
coverage = {extras = ["toml"], version = "*"}
pytest = "*"
pytest-randomly = "*"
pyfakefs = "*"
tomli = {version = "*", markers = "python_version < '3.11'"}
